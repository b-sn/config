Debian soft installer

```
mkdir -p ~/.local/bin
curl -sSL -H "Cache-Control: no-cache" https://raw.githubusercontent.com/b-sn/config/main/installer.sh > ~/.local/bin/installer.sh
chmod 755 ~/.local/bin/installer.sh
```
